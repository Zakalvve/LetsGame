

<RadzenSidebar Responsive="false" Style="@(SidebarExpanded ? "" : "width: max-content")">
    <RadzenPanelMenu DisplayStyle="@(SidebarExpanded ? MenuItemDisplayStyle.IconAndText : MenuItemDisplayStyle.Icon)" ShowArrow="false">
        <RadzenPanelMenuItem Text="Dashboard" Path="" Icon="dashboard" />
        <AuthorizeView>
            <Authorized>
                <RadzenPanelMenuItem Text="Events" Path="events" Icon="event" />
            </Authorized>
        </AuthorizeView>
        <RadzenPanelMenuItem Text="Counter" Path="counter" Icon="add" />
    </RadzenPanelMenu>
</RadzenSidebar>

@code {
    [Parameter]
    public bool SidebarExpanded { get; set; }
}